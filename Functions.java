import java.util.Scanner;

public class Functions {
    // arive function --> O(len(personName))
    // exit function --> O(len(personName))
    // InLib function --> O(len(personName))
    // Borrow book
    // return book
    // TotalTimeInLib --> O(len(personName) + log(Number of Entry and Exit))
    // addNewBook(bookName, count)
    // shouldBring(bookName, personName)
    // allPersonCurrentBooks(personName)
    // allPersonsHaveThisBook(bookName)
    // addNewBook(bookName, count)
    // shouldBring(bookName, personName)
    // allPersonCurrentBooks(personName)
    // allPersonsHaveThisBook(bookName)
    // allPersonsBorrowedThisBook(bookName)
    // allPersonAllTimeBooks(personName)

}